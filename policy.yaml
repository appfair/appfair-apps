# The catalog's rules, in one file, so the scripts that enforce them carry no policy of their own
# and a reviewer can change what the App Fair accepts without touching a line of code.
# `scripts/queue.py validate` reads this on every pull request.

# Every app the App Fair publishes is published by the App Fair: the bundle ids live in this
# namespace, and the App Fair Project is the publisher of record for them
# (https://appfair.org/docs/building/#bundle-id).
id-namespace: org.appfair.app.

# The app token: the name of the app's GitHub organization and repository, and the last segment
# of its bundle id. It is immutable for the life of the app and unique in this catalog, which is
# why it is also the name of the file under apps/.
token-pattern: '^[A-Za-z][A-Za-z0-9-]{1,38}$'

# A submission names a released tag of the app. What gets built has to be what the maintainer
# released, and it has to still be there a year from now.
tag-pattern: '^v[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$'

# ...and the commit that tag pointed at when the submission was written. A tag can be moved, and
# a submission reviewed at one commit and published from another would be a review of nothing, so
# every stage checks out the commit and the tag is held against it.
commit-pattern: '^[0-9a-f]{40}$'

# Where an app can be published, and what each channel is. A submission names its channels under
# the target that builds for them, so the pairing is part of the file rather than a rule that
# checks two lists against each other, and a target can feed more than one channel.
#
#   target        the Day target whose package this channel takes
#   lane          the fastlane lane that uploads it
#   submit-lane   the lane that also asks for review, when a submission sets `submit: true`
#   options       the settings a submission may give this channel
#   status        `ready`, or `planned` for a channel the queue knows of and cannot publish to yet
channels:
  apple-app-store:
    target: ios-uikit
    lane: ios upload
    submit-lane: ios release
    options: [submit, profile-secret]
    status: ready
  google-play-store:
    target: android-mdc
    lane: android upload
    submit-lane: android release
    options: [submit]
    status: ready
  # The channels the catalog is headed for. A submission naming one of these is told that it is
  # coming rather than that it is wrong, and turning one on is this entry plus an arm in
  # .github/actions/sign-submit.
  altstore:
    target: ios-uikit
    options: [submit]
    status: planned
  f-droid:
    target: android-mdc
    options: []
    status: planned
  samsung-galaxy-store:
    target: android-mdc
    options: [submit]
    status: planned

# The Day build flavor every submission is built with. It follows this catalog's name — the
# `appfair-apps` queue builds each app's `appfair` flavor, and a fork called `gamesfair-apps`
# builds its `gamesfair` one — so a fork changes nothing here. That flavor's manifest
# (`Day-<flavor>.toml` in the app) is where the identity the app is published under lives, which
# is why the queue names it and a submission cannot.
#
# flavor: appfair

# Which day CLI the queue builds every submission with. `main` tracks the framework; a released
# version freezes what the catalog builds against.
day-version: main

# An App Store provisioning profile is issued per bundle id, and this catalog issues one during
# the run: `fastlane sigh` asks Apple for a profile for the app being published, against the
# certificate it is about to sign with. Nothing is stored per app, nothing has to be renewed, and
# no wildcard profile has to cover the namespace. An app that needs a particular profile names the
# secret holding it in its own `apple-app-store: profile-secret`.
