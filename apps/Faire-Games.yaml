# yaml-language-server: $schema=../schema/app.schema.json
token: Faire-Games
title: Fair Games
tag: v2.0.2
commit: ae794d9a12a00f7451875d5593438a3534cf666e
distribution:
  android-mdc:
    - google-play-store
  ios-uikit:
    - apple-app-store

# Ask Apple to review the build, rather than leaving it uploaded: the queue then runs the lane
# that attaches the build to the version and submits it. Release stays manual in App Store
# Connect. Play keeps its default, which uploads to the internal track as a draft.
apple-app-store:
  submit: true
